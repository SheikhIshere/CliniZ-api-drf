version: '3.8'

services:
  web:
    build: .
    volumes:
      - .:/app
      # Mount the App folder separately to ensure proper structure
      - ./App:/app/App
    ports:
      - "8000:8000"
    environment:
      # This tells Django to use the SQLite database in the App folder
      - PYTHONPATH=/app/App